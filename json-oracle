SELECT 
    distinct(JSON_VALUE(CAST(DBMS_LOB.SUBSTR(OLD_DATA, 4000, 1) AS VARCHAR2(4000)), '$.national_id')) AS national_id
FROM 
    T_UPAZILA_REMARKS
WHERE CAST(OLD_DATA AS VARCHAR2(4000)) is not null
--and CAST(DBMS_LOB.SUBSTR(OLD_DATA, 4000, 1) AS VARCHAR2(4000)) LIKE '%union_id%';
